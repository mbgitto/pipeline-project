pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Welcome to Jenkins Worllld'
                echo 'Hi i'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
