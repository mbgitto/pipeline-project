pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Welcome to Jenkins World1'
                echo 'Hi i'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
